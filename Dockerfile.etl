FROM python:3.11-slim
ENV PYTHONUNBUFFERED=1
WORKDIR /app
# 1) Install deps
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
# 2) Copy app code
COPY . .
# 3) Default entrypoint (the orchestrator)
CMD ["python", "pipeline.py"]
# a build never runs the CMD
